# Image-Edge-Detection-Comparison
This code snippet performs image edge detection using three different algorithms: Sobel, Prewitt, and Canny. It loads an image, applies each edge detection method, and then displays the original image alongside the results from each algorithm for comparison. This allows for a visual understanding of how each method identifies edges in an image.
